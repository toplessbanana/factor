#  factor

```
OVERVIEW: Print the factors of one or more integers.

USAGE: factor [<integers> ...] [--lcm]

ARGUMENTS:
  <integers>              The integers to factorise.

OPTIONS:
  --lcm                   Print the least common multiple.
  --version               Show the version.
  -h, --help              Show help information.
```
